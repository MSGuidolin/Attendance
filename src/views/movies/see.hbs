<div class="container p-4">
    <div class="col-md-4 mx-auto">
        <div class="card">
            <div class="card-body">
                <form action="/movies/see/{{movie.id}}" method="GET">
                    <div class="form-group text-center">
                        <h3>{{movie.title}}</h3>
                    </div>
                    <div class="form-group">
                        Director: {{movie.director}}
                    </div>
                    <div class="form-group">
                        Género: <a href="/genres/see/{{movie.genreId}}">{{movie.genre}}</a>
                    </div>
                    
                    {{#if movie.synopsis}}
                    <div class="form-group">
                        Sinopsis: {{movie.synopsis}}
                    </div>
                    {{/if}}
                    
                    <div class="col-md-4 mx-auto">
                        <a href="/movies" class="btn btn-primary" role="button">VOLVER</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>